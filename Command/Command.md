# 명령 패턴

구현 요구 사항

1. 플레이어 컨트롤러에는 X,Y,A,B 버튼 4가지가 존재한다.
2. 각각 원하는 버튼에 JUMP(), FIRE_GUN(), LAUNCHER(), SWAP_SWEAPON() 기능이 매핑되게 만든다.
3. 플레이어 뿐만아니라 AI와 같은 게임내에 존재하는 ACTOR와도 호환될 수 있게 만든다.
4. 메멘토 패턴을 활용해 실행취소와 재실행 기능을 구현한다.